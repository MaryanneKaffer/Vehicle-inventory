import { Button } from "@heroui/button";

export default function PostComponent() {
    return (
        <div className="==">
            <Button variant="ghost" color="warning" className="w-full rounded-sm" radius="none" > Register a Vehicle </Button>
        </div>
    );
}